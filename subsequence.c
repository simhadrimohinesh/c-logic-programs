#include<stdio.h>
#include<string.h>
int main()
{
    char s[50];
    scanf("%s",s);
    int i,j,k;
    int len=strlen(s);
    for(i=1;i<=len;i++)
    {
        for(j=0;j)
        {
            for(k)
        }
    }
    return 0;
}